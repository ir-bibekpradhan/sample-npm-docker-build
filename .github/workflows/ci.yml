name: CI

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      IR_API_URL: "https://app.stage.invisirisk.com"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # - name: Setup Node
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 20

      # - name: Install dependencies
      #   run: npm install

      # - name: Build (host)
      #   run: npm run build

      - name: Build Docker image
        run: BUILDKIT=1 docker build -t sample-npm-docker .
